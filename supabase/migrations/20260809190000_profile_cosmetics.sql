alter table public.profiles add column avatar_style text not null default 'initials' check(avatar_style in('initials','leaf','spark','orb')),add column tree_style text not null default 'organic' check(tree_style in('organic','constellation','minimal')),add column profile_layout text not null default 'balanced' check(profile_layout in('balanced','skills_first','achievements_first'));
