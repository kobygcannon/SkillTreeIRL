export type ForecastInput={current:number;target:number;now:Date;unit?:string|null;events:Array<{delta:number;occurredAt:string}>};
export function completionForecast(input:ForecastInput){const positive=input.events.filter(event=>event.delta>0).sort((a,b)=>a.occurredAt.localeCompare(b.occurredAt));if(positive.length<3||input.target<=input.current)return null;const day=86400000,since14=new Date(input.now.getTime()-13*day).toISOString(),elapsed=Math.max(1,(input.now.getTime()-new Date(positive[0].occurredAt).getTime())/day),recent=positive.filter(event=>event.occurredAt>=since14),longRate=positive.reduce((sum,event)=>sum+event.delta,0)/elapsed,recentRate=recent.reduce((sum,event)=>sum+event.delta,0)/14,rates=[longRate,recentRate].filter(rate=>rate>0);if(!rates.length)return null;const remaining=input.target-input.current,fast=Math.max(...rates),slow=Math.min(...rates);return{from:new Date(input.now.getTime()+remaining/fast*day),to:new Date(input.now.getTime()+remaining/slow*day),recentRate,longRate,samples:positive.length,unit:input.unit||"units"}}
