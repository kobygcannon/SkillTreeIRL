import {NextResponse} from "next/server";import {authenticated,failure} from "@/domains/shared/http";import {stripeClient} from "@/lib/stripe";
export async function POST(){try{const auth=await authenticated();if("error" in auth)return auth.error;const stripe=stripeClient(),appUrl=process.env.NEXT_PUBLIC_APP_URL;if(!stripe||!appUrl)return NextResponse.json({error:{code:"BILLING_NOT_CONFIGURED",message:"Billing is not configured"}},{status:503});const {data}=await auth.supabase.from("subscriptions").select("provider_customer_id").maybeSingle();if(!data?.provider_customer_id)return NextResponse.json({error:{code:"NO_BILLING_ACCOUNT",message:"Start a subscription before opening billing management"}},{status:404});const session=await stripe.billingPortal.sessions.create({customer:data.provider_customer_id,return_url:`${appUrl}/app`});return NextResponse.json({data:{url:session.url}})}catch(error){return failure(error)}}
