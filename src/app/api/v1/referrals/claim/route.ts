import {cookies} from "next/headers";import {NextResponse} from "next/server";import {authenticated,failure} from "@/domains/shared/http";
export async function POST(){const auth=await authenticated();if("error" in auth)return auth.error;const jar=await cookies(),code=jar.get("skilltree_referral")?.value;if(!code)return NextResponse.json({data:{claimed:false}});const {error}=await auth.supabase.rpc("claim_referral",{p_code:code});if(error)return failure(error);const response=NextResponse.json({data:{claimed:true}});response.cookies.set("skilltree_referral","",{path:"/",maxAge:0,httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production"});return response}
