import {NextResponse} from "next/server";import {authenticated,failure} from "@/domains/shared/http";
export async function GET(){const auth=await authenticated();if("error" in auth)return auth.error;const {data,error}=await auth.supabase.from("friendships").select("id,requester_id,addressee_id,status,created_at,updated_at").order("updated_at",{ascending:false});if(error)return failure(error);return NextResponse.json({data,userId:auth.userId})}
export async function POST(request:Request){try{const auth=await authenticated();if("error" in auth)return auth.error;const body=await request.json() as {userId?:string};if(!body.userId||body.userId===auth.userId)return NextResponse.json({error:{code:"INVALID_FRIEND",message:"Choose another user"}},{status:422});const {data,error}=await auth.supabase.from("friendships").insert({requester_id:auth.userId,addressee_id:body.userId}).select().single();if(error)return failure(error);return NextResponse.json({data},{status:201})}catch(error){return failure(error)}}
