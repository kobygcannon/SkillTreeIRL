import {NextResponse} from "next/server";import {authenticated} from "@/domains/shared/http";import {createAdminClient} from "@/lib/supabase/admin";import {encryptSecret,hashToken} from "@/lib/security/tokens";
export async function GET(request:Request){const auth=await authenticated();if("error" in auth)return auth.error;const url=new URL(request.url),code=url.searchParams.get("code"),state=url.searchParams.get("state"),admin=createAdminClient(),clientId=process.env.GITHUB_CLIENT_ID,clientSecret=process.env.GITHUB_CLIENT_SECRET,appUrl=process.env.NEXT_PUBLIC_APP_URL;if(!code||!state||!admin||!clientId||!clientSecret||!appUrl)return NextResponse.redirect(new URL("/app?integration=failed",appUrl||url.origin));const {data:stateRow}=await admin.from("oauth_states").select("id,user_id,expires_at,used_at").eq("state_hash",hashToken(state)).eq("provider","github").single();if(!stateRow||stateRow.user_id!==auth.userId||stateRow.used_at||new Date(stateRow.expires_at)<new Date())return NextResponse.redirect(new URL("/app?integration=invalid_state",appUrl));await admin.from("oauth_states").update({used_at:new Date().toISOString()}).eq("id",stateRow.id);const tokenResponse=await fetch("https://github.com/login/oauth/access_token",{method:"POST",headers:{accept:"application/json","content-type":"application/json"},body:JSON.stringify({client_id:clientId,client_secret:clientSecret,code,redirect_uri:`${appUrl}/api/v1/integrations/github/callback`})});const token=await tokenResponse.json() as {access_token?:string;scope?:string;error?:string};if(!token.access_token)return NextResponse.redirect(new URL("/app?integration=token_failed",appUrl));const userResponse=await fetch("https://api.github.com/user",{headers:{accept:"application/vnd.github+json",authorization:`Bearer ${token.access_token}`,"x-github-api-version":"2022-11-28","user-agent":"SkillTree-IRL"}});const githubUser=await userResponse.json() as {id?:number;login?:string};if(!githubUser.id)return NextResponse.redirect(new URL("/app?integration=profile_failed",appUrl));const {data:integration,error}=await admin.from("integrations").upsert({user_id:auth.userId,provider:"github",status:"connected",scopes:(token.scope||"").split(",").map(x=>x.trim()).filter(Boolean),external_account_id:String(githubUser.id),metadata:{login:githubUser.login}},{onConflict:"user_id,provider,external_account_id"}).select("id").single();if(error||!integration)return NextResponse.redirect(new URL("/app?integration=save_failed",appUrl));await admin.rpc("store_integration_credentials",{p_integration_id:integration.id,p_user_id:auth.userId,p_access:encryptSecret(token.access_token),p_refresh:null,p_expires_at:null});return NextResponse.redirect(new URL("/app?integration=github_connected",appUrl))}
