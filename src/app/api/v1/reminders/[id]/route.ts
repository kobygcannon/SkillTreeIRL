import {NextResponse} from "next/server";import {authenticated,failure} from "@/domains/shared/http";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const auth=await authenticated();if("error" in auth)return auth.error;const {id}=await params,body=await request.json() as {title?:string;schedule?:unknown;timezone?:string;enabled?:boolean;nextRunAt?:string|null};const update:Record<string,unknown>={updated_at:new Date().toISOString()};if(body.title!==undefined)update.title=body.title.trim();if(body.schedule!==undefined)update.schedule=body.schedule;if(body.timezone!==undefined)update.timezone=body.timezone;if(body.enabled!==undefined)update.enabled=body.enabled;if(body.nextRunAt!==undefined)update.next_run_at=body.nextRunAt;const {data,error}=await auth.supabase.from("reminders").update(update).eq("id",id).select().single();if(error)return failure(error);return NextResponse.json({data})}catch(error){return failure(error)}}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){const auth=await authenticated();if("error" in auth)return auth.error;const {id}=await params,{error}=await auth.supabase.from("reminders").delete().eq("id",id);if(error)return failure(error);return NextResponse.json({data:{deleted:true}})}
