import {NextResponse} from "next/server";import {authenticated,failure} from "@/domains/shared/http";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const auth=await authenticated();if("error" in auth)return auth.error;const {id}=await params;const body=await request.json() as {enabled?:boolean};if(typeof body.enabled!=="boolean")return NextResponse.json({error:{code:"VALIDATION_FAILED",message:"Enabled must be true or false"}},{status:422});const {data,error}=await auth.supabase.from("webhooks").update({enabled:body.enabled}).eq("id",id).select("id,url,events,enabled").single();if(error)return failure(error);return NextResponse.json({data})}catch(error){return failure(error)}}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){const auth=await authenticated();if("error" in auth)return auth.error;const {id}=await params;const {error}=await auth.supabase.from("webhooks").delete().eq("id",id);if(error)return failure(error);return new NextResponse(null,{status:204})}
