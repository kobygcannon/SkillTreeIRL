import {createHmac} from "node:crypto";import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";import {decryptSecret} from "@/lib/security/tokens";import {reportProductionError} from "@/lib/monitoring";import {assertPublicHttpsUrl} from "@/lib/security/outbound-url";
export {POST as GET};
export async function POST(request:Request){const expected=process.env.CRON_SECRET,provided=request.headers.get("authorization");if(!expected||provided!==`Bearer ${expected}`)return NextResponse.json({error:{code:"UNAUTHORIZED",message:"Invalid worker credential"}},{status:401});const admin=createAdminClient();if(!admin)return NextResponse.json({error:{code:"NOT_CONFIGURED"}},{status:503});const {data:deliveries,error}=await admin.from("webhook_deliveries").select("id,payload,attempts,webhooks(url,secret_ciphertext)").in("status",["pending","retrying"]).lte("next_attempt_at",new Date().toISOString()).order("created_at").limit(25);if(error)return NextResponse.json({error:{code:"DATABASE_ERROR"}},{status:500});let delivered=0,failed=0;for(const delivery of deliveries||[]){const hook=delivery.webhooks as unknown as {url:string;secret_ciphertext:string};const body=JSON.stringify(delivery.payload),timestamp=Math.floor(Date.now()/1000),signature=createHmac("sha256",decryptSecret(hook.secret_ciphertext)).update(`${timestamp}.${body}`).digest("hex");try{const target=await assertPublicHttpsUrl(hook.url);const response=await fetch(target,{method:"POST",redirect:"error",headers:{"content-type":"application/json","user-agent":"SkillTree-Webhooks/1.0","x-skilltree-id":delivery.id,"x-skilltree-timestamp":String(timestamp),"x-skilltree-signature":`v1=${signature}`},body,signal:AbortSignal.timeout(10000)});if(response.ok){await admin.from("webhook_deliveries").update({status:"delivered",attempts:delivery.attempts+1,response_status:response.status,delivered_at:new Date().toISOString(),last_error:null}).eq("id",delivery.id);delivered++}else throw new Error(`HTTP ${response.status}`)}catch(error){const attempts=delivery.attempts+1,dead=attempts>=5;await admin.from("webhook_deliveries").update({status:dead?"failed":"retrying",attempts,last_error:error instanceof Error?error.message.slice(0,500):"Delivery failed",next_attempt_at:new Date(Date.now()+Math.min(3600000,30000*2**attempts)).toISOString()}).eq("id",delivery.id);await reportProductionError({message:error instanceof Error?error.message:"Webhook delivery failed",stack:error instanceof Error?error.stack:undefined,source:"provider",route:"webhook-worker",context:{deliveryId:delivery.id,attempts,dead}});failed++}}return NextResponse.json({data:{processed:(deliveries||[]).length,delivered,failed}})}
