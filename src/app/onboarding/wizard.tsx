/* eslint-disable @next/next/no-location-assign-relative-destination */
"use client";import {useState} from "react";import {ArrowLeft,ArrowRight,Check,Leaf,Sparkles} from "lucide-react";import "./wizard.css";
const categories=["Health & Fitness","Money","Career","Learning","Business","Creative","Personal","Relationships","Practical Skills","Something Else"];
const templates:Record<string,string[]>={"Health & Fitness":["Exercise consistently","Run 5K","Run 10K","Gain strength","Improve endurance"],Money:["Build an emergency fund","Save a specific amount","Pay off debt","Increase income","Invest consistently"],Business:["Start a business","Launch a product","Make first sale","Reach a revenue target","Build an audience"],Learning:["Learn a language","Complete a course","Read more books","Master a new subject"],Career:["Earn a promotion","Find a new role","Build my portfolio","Develop leadership skills"]};
export default function Onboarding(){const [step,setStep]=useState(1),[category,setCategory]=useState(""),[title,setTitle]=useState(""),[measurement,setMeasurement]=useState("open_ended"),[target,setTarget]=useState(""),[unit,setUnit]=useState(""),[saving,setSaving]=useState(false),[error,setError]=useState("");const create=async()=>{setSaving(true);setError("");const response=await fetch("/api/v1/goals",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title,category,measurement,targetValue:target?Number(target):undefined,currentValue:0,unit:unit||undefined,priority:"focus"})});if(!response.ok){const body=await response.json();setError(body.error?.message||"Your goal could not be created.");setSaving(false);return}location.href="/app"};return <main className="onboard"><header><div className="onboard-logo"><span><Leaf/></span>SkillTree <em>IRL</em></div><div className="steps"><i className={step>=1?"active":""}/><i className={step>=2?"active":""}/><i className={step>=3?"active":""}/></div><button onClick={()=>location.href="/app"}>Do this later</button></header><section>{step===1&&<div className="onboard-card"><span className="onboard-icon"><Sparkles/></span><p className="kicker">LET’S START SMALL</p><h1>What are you working toward?</h1><p>Choose one area for now. Your SkillTree can grow in any direction later.</p><div className="category-grid">{categories.map(c=><button key={c} className={category===c?"selected":""} onClick={()=>setCategory(c)}>{category===c&&<Check/>}{c}</button>)}</div><button className="continue" disabled={!category} onClick={()=>setStep(2)}>Continue <ArrowRight/></button></div>}{step===2&&<div className="onboard-card"><button className="back" onClick={()=>setStep(1)}><ArrowLeft/> Back</button><p className="kicker">CHOOSE A STARTING POINT</p><h1>What would meaningful progress look like?</h1><p>Pick a suggestion or write your own. You can change it without losing history.</p><div className="templates">{(templates[category]||["Build a meaningful personal goal","Create my own goal"]).map(t=><button key={t} className={title===t?"selected":""} onClick={()=>setTitle(t)}>{t}<ArrowRight/></button>)}</div><label className="custom">Or describe your own goal<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Restore my grandfather’s classic car" maxLength={180}/></label><button className="continue" disabled={!title.trim()} onClick={()=>setStep(3)}>Set up progress <ArrowRight/></button></div>}{step===3&&<div className="onboard-card narrow"><button className="back" onClick={()=>setStep(2)}><ArrowLeft/> Back</button><p className="kicker">ONE LAST DETAIL</p><h1>How should progress be measured?</h1><p>SkillTree suggested a model. Adjust it if another fits your goal better.</p><label>Measurement<select value={measurement} onChange={e=>setMeasurement(e.target.value)}><option value="open_ended">Open-ended development</option><option value="numeric">Numeric target</option><option value="currency">Currency target</option><option value="percentage">Percentage</option><option value="frequency">Frequency</option><option value="duration">Duration</option><option value="milestones">Milestones</option><option value="binary">Binary completion</option><option value="recurring">Recurring consistency</option><option value="composite">Composite goal</option><option value="custom">Custom measurement</option></select></label>{!["open_ended","binary","milestones"].includes(measurement)&&<div className="target-row"><label>Target<input type="number" min="0" value={target} onChange={e=>setTarget(e.target.value)}/></label><label>Unit<input value={unit} onChange={e=>setUnit(e.target.value)} placeholder={measurement==="currency"?"GBP":"sessions"}/></label></div>}<div className="goal-preview"><small>YOUR FIRST FOCUS GOAL</small><b>{title}</b><span>{category} · Progress can be revised later</span></div>{error&&<div className="onboard-error">{error}</div>}<button className="continue" disabled={saving} onClick={create}>{saving?"Building your SkillTree…":"Build my SkillTree"} <Sparkles/></button></div>}</section></main>}
