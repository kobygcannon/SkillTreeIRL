import {describe,expect,it} from "vitest";import {classifySyncResponse} from "./queue";
describe("offline sync decisions",()=>{it("removes only confirmed responses",()=>{expect(classifySyncResponse(201)).toBe("confirmed");expect(classifySyncResponse(409)).toBe("conflict")});it("pauses for authentication and retries transient failures",()=>{expect(classifySyncResponse(401)).toBe("authentication");expect(classifySyncResponse(429)).toBe("retry");expect(classifySyncResponse(503)).toBe("retry")});it("requires user resolution for permanent validation failures",()=>{expect(classifySyncResponse(400)).toBe("conflict");expect(classifySyncResponse(422)).toBe("conflict")})});
