name: Deploy staging and smoke test
on:
  workflow_run:
    workflows: ["Production quality gate"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  staging:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: staging
    concurrency:
      group: skilltree-staging
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: pnpm/action-setup@v4
        with: { version: 11.16.0 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - uses: supabase/setup-cli@v1
        with: { version: 2.112.0 }
      - run: pnpm install --frozen-lockfile
      - name: Apply tested database migrations to staging
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          supabase link --project-ref "$SUPABASE_PROJECT_ID" --password "$SUPABASE_DB_PASSWORD"
          supabase db push --linked --include-all --password "$SUPABASE_DB_PASSWORD"
      - run: pnpm dlx vercel@50.15.1 pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
      - run: pnpm dlx vercel@50.15.1 build --token=${{ secrets.VERCEL_TOKEN }}
        env:
          APP_ENV: staging
          APP_RELEASE: ${{ github.event.workflow_run.head_sha || github.sha }}
          NEXT_PUBLIC_APP_ENV: staging
          NEXT_PUBLIC_APP_RELEASE: ${{ github.event.workflow_run.head_sha || github.sha }}
          SENTRY_ENVIRONMENT: staging
          SENTRY_RELEASE: ${{ github.event.workflow_run.head_sha || github.sha }}
      - id: deploy
        run: echo "url=$(pnpm dlx vercel@50.15.1 deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }})" >> "$GITHUB_OUTPUT"
      - name: Verify the exact staging release
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.url }}
          EXPECTED_RELEASE: ${{ github.event.workflow_run.head_sha || github.sha }}
        run: |
          curl --fail --retry 5 --retry-delay 5 "$DEPLOYMENT_URL/health/live" --output live.json
          node -e 'const r=require("./live.json"); if(r.status!=="ok"||r.release!==process.env.EXPECTED_RELEASE) process.exit(1)'
      - run: curl --fail --retry 5 --retry-delay 5 "${{ steps.deploy.outputs.url }}/health/ready"
      - run: curl --fail --retry 5 --retry-delay 5 "${{ steps.deploy.outputs.url }}/sign-in"
